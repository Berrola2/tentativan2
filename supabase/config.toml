# ==============================================================================
# CONFIGURAÇÃO DECLARATIVA DO SUPABASE CLI — VISTORIA YZZY
# ==============================================================================

project_id = "wyyigrlqxwjxjqkazeof"

[api]
enabled = true
port = 54321
schemas = ["public", "storage", "graphql_public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[auth]
enabled = true
site_url = "http://localhost:5173"
additional_redirect_urls = ["https://*.vercel.app"]
jwt_expiry = 3600
enable_signup = false

# Edge Function pública de Login YZZY (pré-autenticação)
[functions.login-with-yzzy]
verify_jwt = false

# Edge Function administrativa para gestão de empresas e funcionários
[functions.admin-manage-user]
verify_jwt = false

# Edge Function para alteração de senha
[functions.change-password]
verify_jwt = false
